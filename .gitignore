# Prevent accidentally regenerating hardware config at the repository root.
# Canonical tracked host configs live under hosts/<hostname>/.
/hardware-configuration.nix

.codegraph/
.cursor/
.omc/
result
result-*
